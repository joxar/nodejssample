platform: linux

image_resource:
  type: docker-image
  source: {repository: busybox}

jobs:
- name: run
  plan:
  - task: annoy
    config:
      platform: linux
      image_resource:
        type: docker-image
        source: {repository: ubuntu}
      run:
        path: sh
        args: ["test.sh"]
